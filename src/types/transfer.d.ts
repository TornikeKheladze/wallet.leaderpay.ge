export type TransferPay = {
  wallet_number: string;
  amount: string;
  currency_id: string;
  sms: string;
};

export type Cashbox = {
  address: string;
  city: string;
  lat: string;
  lng: string;
  work: string;
};
